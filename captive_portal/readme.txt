Make sure to have tools installed:
  -python3
  -xterm
  -apache2


Usage:
  To run:
    use commnad: "bash captive_port.sh"                   # Start captive portal with accesspoint, dnsserver and dhcpserver as well
    otherwise use: "bash cp_with_dhcp_without_ap.sh"      # Start captive portal with dns server and dhcp server without access point
